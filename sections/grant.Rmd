
# Grants and Awards

```{r}
vroom::vroom("data/grant.csv") %>%
    mutate(
        funder = if_else(!is.na(where) & where != "", glue::glue("{with}, {where}"), with),
        amount = why
    ) %>%
    vitae::detailed_entries(what = what, when = when, with = funder, where = amount)
```
